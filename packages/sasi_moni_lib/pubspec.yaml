name: sasi_moni_lib
description: SASI Moni — shared domain library for white-label Flutter apps.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  equatable: ^2.0.5
  intl: ^0.19.0

dev_dependencies:
  test: ^1.24.0
  mocktail: ^1.0.4
